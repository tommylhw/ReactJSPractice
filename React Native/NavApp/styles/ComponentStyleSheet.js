import { StyleSheet } from "react-native";

const ComponentStyleSheet = StyleSheet.create({
  customNavBtnContainer: {
    position: 'absolute',
    top: 15,
    right: 15,
  },

  backBtn: {
    position: 'absolute',
    top: 15,
    left: 15,
  },
});

export { ComponentStyleSheet }